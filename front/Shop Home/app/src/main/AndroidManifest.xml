<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--javaファイルの追加時には<activity></activity>で囲む -->
        <!--android:label="@string/app_name">ここでアプリの上のテキストを呼び出す-->
        <activity
            android:name=".ShopHome"
            android:label="@string/app_shophome">
        </activity>

<!--        ShopSampleの記述-->
        <activity
            android:name=".ShopSample"
            android:label="@string/app_category">
        </activity>

<!--        ShopYouserの記述-->
        <activity
            android:name=".ShopYouser"
            android:label="@string/app_youser">
        </activity>

        <!--        ShopNoticeの記述-->
        <activity
            android:name=".ShopNoitice"
            android:label="@string/app_notice">
        </activity>

    </application>

</manifest>